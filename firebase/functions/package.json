{
  "name": "district-functions",
  "version": "1.0.0",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "deploy": "firebase deploy --only functions",
    "lint": "eslint src/",
    "serve": "npm run build && firebase emulators:start --only functions"
  },
  "engines": {
    "node": "22"
  },
  "dependencies": {
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^7.2.5"
  },
  "devDependencies": {
    "@types/node": "^22.20.0",
    "eslint": "^9.0.0",
    "typescript": "^5.5.0"
  }
}
